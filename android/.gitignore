# Build outputs
/build/
/app/build/
.gradle/

# Bundled web assets are generated by tools/sync-web-assets.sh before each
# build (so the app is never duplicated in the repo).
/app/src/main/assets/www/

# Local SDK pointer (each machine / CI sets its own)
local.properties

# IDE
/.idea/
*.iml
.DS_Store
